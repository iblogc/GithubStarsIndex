# ============================================================
# GitHub Stars Index 行为调优配置
# ============================================================
# 本文件存放非敏感的行为参数。
# 敏感核心配置（API Key 等）通过 GitHub Secrets/Variables 管理。
# ============================================================

ai:
  model: "gpt-4o-mini"         # 默认模型
  max_readme_length: 8000       # README 截断长度
  timeout: 60                   # 请求超时（秒）
  max_retries: 3                # 最大重试次数
  concurrency: 1                # 默认并发数（也可通过 MAX_CONCURRENCY 环境变量覆盖）

output:
  file_path: "stars.md"         # 生成的 Markdown 文件基准名 (自动补全 _zh/_en)

vault_sync:
  commit_message: "🤖 自动更新 GitHub Stars 摘要"
  # 默认路径在 GitHub Variables 中的 VAULT_FILE_PATH 配置，此处留空
  default_file_path: "GitHub-Stars/stars.md"

# pages_sync 默认值已内置在脚本中 (dist/index.html)，通常无需在此配置